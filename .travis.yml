language: go
sudo: required
services:
- docker
go:
- 1.6
install: make
script:
- make
- docker build -t ${DOCKER_USER}/hello .
- docker login -u ${DOCKER_USER} -p ${DOCKER_PASS}
- docker push ${DOCKER_USER}/hello
notifications:
  email: false